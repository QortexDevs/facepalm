<label>
    <input type="hidden" name="{{ inputName }}" value="0" checked/>
    <input type="checkbox" name="{{ inputName }}" value="1"
           {% if object.__get(field) %}checked{% endif %} />
    {{ parameters.title }}
</label>
